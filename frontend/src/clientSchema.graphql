# this is for local state management (https://relay.dev/docs/en/local-state-management)
type Note {
  id: ID!
  title: String
  body: String
}

extend type UserType {
  authToken: String
  notes: [Note]
}
